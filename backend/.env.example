# ─── Ganesh Trades Backend Environment Variables ───
# Copy this file to .env and fill in real values.
# NEVER commit .env with real secrets to version control.

PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<dbname>?retryWrites=true&w=majority

# JWT authentication secret (min 32 chars in production)
JWT_SECRET=<generate-a-strong-random-secret-min-32-chars>
JWT_EXPIRE=7d

# Frontend URL for CORS (comma-separated for multiple origins)
# Development: http://localhost:5173
# Production:  https://your-app.vercel.app,https://your-app.onrender.com
FRONTEND_URL=http://localhost:5173

# ─── Independent OTP Microservice Integration ───
# URL of the deployed OTP Server microservice
OTP_SERVICE_URL=http://localhost:5001
# Server-to-Server Secret API Key (must match OTP_SERVICE_API_KEY on OTP Server)
OTP_SERVICE_API_KEY=your_secure_server_to_server_api_key_here

